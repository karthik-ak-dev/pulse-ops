// API Routes Index
// This file will export the main API router

import { Router } from 'express';

const router = Router();

// API routes will be mounted here
// router.use('/v1', v1Routes);

export default router;
